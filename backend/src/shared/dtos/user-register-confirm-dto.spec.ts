import { UserRegisterConfirmDto } from './user-register-confirm-dto';

describe('UserRegisterConfirmDto', () => {
  it('should be defined', () => {
    expect(new UserRegisterConfirmDto()).toBeDefined();
  });
});
