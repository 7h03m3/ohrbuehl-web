import { UserNameValidDirective } from './user-name-valid.directive';

describe('UserNameValidDirective', () => {
  it('should create an instance', () => {
    const directive = new UserNameValidDirective();
    expect(directive).toBeTruthy();
  });
});
