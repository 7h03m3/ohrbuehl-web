import { BusinessHourAdminApi } from './business-hour-admin-api';

describe('BusinessHourAdminApi', () => {
  it('should create an instance', () => {
    expect(new BusinessHourAdminApi()).toBeTruthy();
  });
});
