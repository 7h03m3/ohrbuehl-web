import { ContactMessageApi } from './contact-message-api';

describe('ContactMessageApi', () => {
  it('should create an instance', () => {
    expect(new ContactMessageApi()).toBeTruthy();
  });
});
