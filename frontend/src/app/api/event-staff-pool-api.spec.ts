import { EventStaffPoolApi } from './event-staff-pool-api';

describe('EventStaffPoolApi', () => {
  it('should create an instance', () => {
    expect(new EventStaffPoolApi()).toBeTruthy();
  });
});
