import { OrganizationMemberApi } from './organization-member-api';

describe('OrganizationMemberApi', () => {
  it('should create an instance', () => {
    expect(new OrganizationMemberApi()).toBeTruthy();
  });
});
