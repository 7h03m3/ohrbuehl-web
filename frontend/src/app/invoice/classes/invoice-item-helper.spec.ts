import { InvoiceItemHelper } from './invoice-item-helper';

describe('InvoiceItemHelper', () => {
  it('should create an instance', () => {
    expect(new InvoiceItemHelper()).toBeTruthy();
  });
});
