import { EventStaffStatisticItem } from './event-staff-statistic-item';

describe('EventStaffStatisticItem', () => {
  it('should create an instance', () => {
    expect(new EventStaffStatisticItem()).toBeTruthy();
  });
});
