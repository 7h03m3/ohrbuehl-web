import { GermanMatPaginatorIntl } from './german-mat-paginator-intl';

describe('GermanMatPaginatorIntl', () => {
  it('should create an instance', () => {
    expect(new GermanMatPaginatorIntl()).toBeTruthy();
  });
});
