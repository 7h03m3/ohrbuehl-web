import { ShotNumberBilling } from './shot-number-billing';

describe('ShotNumberBilling', () => {
  it('should create an instance', () => {
    expect(new ShotNumberBilling()).toBeTruthy();
  });
});
